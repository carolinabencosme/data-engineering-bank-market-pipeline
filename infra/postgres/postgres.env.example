# Optional overrides for the postgres service (merged after project root .env).
# Define POSTGRES_*, POSTGRES_PGDATA, and AIRFLOW_DB_* in the project .env file.
# Docker Compose does not expand ${VAR} inside env_file values; use real values in .env only.
