# Optional ClickHouse overrides (merged after project root .env).
# Define CLICKHOUSE_* in the project .env file.
# Docker Compose does not expand ${VAR} inside env_file values.

CLICKHOUSE_DEFAULT_ACCESS_MANAGEMENT=1
